import {logger} from "../index";

class HttpClient {
    constructor() {
    }

    connect(url, configuration) {
        try {

        } catch (error) {
            logger.error(error);
        }
    }
}

export default HttpClient;
